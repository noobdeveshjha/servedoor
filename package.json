{
  "name": "servedoor-root",
  "version": "1.0.0",
  "description": "Root folder - Use subfolders (customer-app, backend, etc.)",
  "scripts": {
    "start-customer": "cd customer-app && npx expo start",
    "start-backend": "cd backend && node server.js"
  }
}